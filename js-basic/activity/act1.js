// Exercícios do dia:

// 1) Given this array of fruits with 3 elements:

// const fruits = ['orange', 'banana', 'apple'];
const fruits = ["orange", "banana", "apple"];
// A) Get the last element of the array

const getFruits = fruits[2];
console.log("Last element of the array:", getFruits);

// B) Get the first element of the array
const getFruits1 = fruits[0];
console.log("First element of the array:", getFruits1);

// C) Get the last element of the array ever added an item
const getFruits2 = fruits[fruits.length - 1];
console.log("Last element of the array:", getFruits2);
